version: "1"
pipelines:
  - name: pharmacy-backend-deploy
    services:
      - name: pharmacy-api
        dockerfile: Dockerfile
        port: 5050
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          PORT: 5050
        health-check:
          endpoint: /health
          interval: 30
          timeout: 10
